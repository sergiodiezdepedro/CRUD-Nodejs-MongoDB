extends layout

block content
    h1.cabecera-pagina #{title}
    ul.list-group
        each article, i in articles
            li.list-group-item
                a(href="/articles/"+article._id)= article.title
